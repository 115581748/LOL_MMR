# 峡谷行为基准

这是 `https://115581748.github.io/model/` 的静态站点源文件。

发布方式：

1. 在 GitHub 新建公开仓库 `115581748/model`。
2. 将本目录内的文件放到仓库 `main` 分支根目录。
3. 在仓库 Settings → Pages 中选择 **Deploy from a branch**。
4. Branch 选择 `main`，目录选择 `/(root)`。

模型数据已经去标识化，不包含 Riot API Key 或玩家 PUUID。
